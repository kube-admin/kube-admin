# 后端配置示例
# 复制此文件为 .env 并修改配置

# 服务端口
PORT=8080

# Kubeconfig 路径 (可选,默认使用 ~/.kube/config)
# KUBECONFIG=/path/to/your/kubeconfig

# JWT 密钥 (生产环境必须修改!)
JWT_SECRET=your-very-secret-key-change-in-production
